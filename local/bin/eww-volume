#!/bin/bash

# Get current volume
VOLUME=$(pactl get-sink-volume @DEFAULT_SINK@ | grep -Po '\d+(?=%)' | head -1)

# Update EWW
eww update volume-percent=$VOLUME
eww update island-mode="volume"

# Reset to idle after 2 seconds
(sleep 2 && eww update island-mode="idle") &
