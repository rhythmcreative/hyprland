# Hyprland enhanced colors generated by pywal for hyprlock
# Generated automatically - DO NOT EDIT MANUALLY

# Main color palette from pywal
$color0 = rgb({color0.strip})    # Dark background
$color1 = rgb({color1.strip})    # Primary accent (red/pink tones)
$color2 = rgb({color2.strip})    # Secondary accent (green tones)
$color3 = rgb({color3.strip})    # Tertiary accent (yellow/orange tones)
$color4 = rgb({color4.strip})    # Quaternary accent (blue tones)
$color5 = rgb({color5.strip})    # Quinary accent (magenta/purple tones)
$color6 = rgb({color6.strip})    # Senary accent (cyan tones)
$color7 = rgb({color7.strip})    # Light foreground
$color8 = rgb({color8.strip})    # Dark gray
$color9 = rgb({color9.strip})    # Bright red (error color)
$color10 = rgb({color10.strip})  # Bright green (success color)
$color11 = rgb({color11.strip})  # Bright yellow (warning color)
$color12 = rgb({color12.strip})  # Bright blue
$color13 = rgb({color13.strip})  # Bright magenta
$color14 = rgb({color14.strip})  # Bright cyan
$color15 = rgb({color15.strip})  # Bright white

# Main colors
$background = rgb({background.strip})
$foreground = rgb({foreground.strip})
$cursor = rgb({cursor.strip})

# Enhanced colors for hyprlock elements
$primary_accent = $color1        # Main accent color for borders and highlights
$secondary_accent = $color2      # Secondary color for success states
$error_color = $color9          # Error/fail color
$warning_color = $color11       # Warning/caps lock color
$text_color = $foreground       # Main text color
$dim_text = $color7             # Dimmed text color
$bg_dark = $color0              # Dark background variant
$bg_medium = $color8            # Medium background variant

# Hyprland window border colors (variables for usage in main config)
$active_border_col = $primary_accent
$inactive_border_col = $color8
$shadow_col = rgba({color0.strip}CC)
