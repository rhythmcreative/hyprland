#!/bin/bash
# Wrapper para ejecutar sddm-auto-sync-local con privilegios sin interacción
# Usa sudo con la entrada NOPASSWD configurada por el instalador

exec sudo "$HOME"/.local/bin/sddm-auto-sync-local "$@"
