#!/bin/bash

# Pywal hook to automatically sync VS Code colors
# This script is called by pywal after generating new colors

# Execute our VS Code sync script
"$HOME/.config/wal/scripts/sync-vscode.sh"
